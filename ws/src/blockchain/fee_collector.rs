// use std::pin::Pin;
// use async_stream::try_stream;
// use futures::Stream;
// use eyre::Result;

// pub struct FeeCollector {
//   stream: Pin<Box<dyn Stream<Item = Result<()>> + Send>>,
// }

// impl FeeCollector {
//   pub fn collect() -> Self {

//   }
// }
